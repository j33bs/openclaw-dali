# Interbeing Watcher v0 Evidence

This bundle captures the executed watcher hardening smoke from
`scripts/dev/test_interbeing_watcher_v0.ts`.

Included here:

- processed, failed, skipped, replayed, and forced-reprocess receipt examples
- status, list, and verify JSON snapshots
- watcher log and state snapshots
- sample processed and failed envelope artifacts
- lifecycle artifacts emitted by the real local lifecycle adapter

This bundle reflects actual local runs only. It does not invent transport,
auth, or non-local execution semantics.
