# Rollback Notes

This pass is documentation and evidence only.

Preferred rollback after commit:

- `git revert --no-edit HEAD`

Manual rollback before sharing the commit:

- `git rm -r workspace/audit/_evidence/dali-interbeing-alignment`
